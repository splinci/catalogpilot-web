# SPLINCI COMMERCE OS — CAPACITY & PERFORMANCE READINESS

## 1. Concurrency & Connection Governance
- Application capacity configured to handle 500 concurrent connections with tenant bulkhead limits (`max 50 slots/tenant`).
- Synthetic load testing in live production is flagged as `EXTERNAL_VERIFICATION_REQUIRED`.
