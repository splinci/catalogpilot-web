# SPLINCI COMMERCE OS — ALERT ROUTING & ON-CALL RUNBOOK

## 1. On-Call Escalation & Alert Routing
- Integrates SEV_1 and SEV_2 incidents with external alert routing providers (`evaluateAlertRoutingReadiness`).
- Real webhook delivery verification is marked as `EXTERNAL_VERIFICATION_REQUIRED` pending live API key provisioning.
