import { PageHero } from "@/components/layout/PageHero";

export default function WelcomeBanner() {
  return (
    <PageHero
      title="Executive Overview 👋"
      description="Real-time operational metrics, AI inventory insights, and channel performance."
    />
  );
}